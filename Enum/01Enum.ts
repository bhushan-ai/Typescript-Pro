//Typescript Enum

//An enum is a special "class" that represents a group of constants (unchangeable variables).

//Comes in two flavors 1. Numeric 2. String

//Numeric

enum Fourdirections {
  east = 1,
  west,
  north,
  south,
}

//let Fourdirections = Fourdirections.est`
console.log(Fourdirections.south);
